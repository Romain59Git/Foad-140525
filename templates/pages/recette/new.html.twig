{% extends 'base.html.twig' %}

{% block title %}Création d'une recette{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>Création d'une recette</h1>

    {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.price) }}
        <button class="btn btn-primary mt-3">Créer une recette</button>
    {{ form_end(form) }}
</div>
{% endblock %}
